#include<stdio.h>
int main()
{
	int n,i,flag=0;
	printf("enter the number");
	scanf("%d",&n);
	for(i=2;i<=2/n;i++)
	{
		if(n%i==0)
		{
			flag=1;
			break;
		}
    }
    if(flag==0)
        printf("%d it is prime",n);
    else
    	printf("%d it is  not prime",n);
    return 0;
}
